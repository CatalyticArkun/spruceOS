# Pyright Configuration

Category
- Pyright configuration updates to align with the new typing patterns.

Method
- Adjust config settings to match the updated type checking expectations.

Examples
- `pyrightconfig.json`: updated config values and paths.
