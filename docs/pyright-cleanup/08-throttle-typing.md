# Throttle Utility Typing

Category
- Refine throttle decorator typing to keep classmethod compatibility without ignores.

Method
- Adjust callable signatures and generics to match decorator usage.
- Export throttle utilities explicitly in `utils` package.

Examples
- `main-ui/utils/throttle.py`: typed decorator signature.
- `main-ui/utils/__init__.py`: module export for static checkers.
